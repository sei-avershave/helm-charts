apiVersion: v1
kind: Secret
metadata:
  name: {{ include "cass.fullname" . }}
  labels:
    {{- include "cass.labels" . | nindent 4 }}
type: Opaque
stringData:
{{- range $key, $val := .Values.env }}
  {{ $key }}: {{ $val | quote }}
{{- end }}