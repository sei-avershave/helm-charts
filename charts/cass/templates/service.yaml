apiVersion: v1
kind: Service
metadata:
  name: {{ include "cass.fullname" . }}
  labels:
    {{- include "cass.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "cass.selectorLabels" . | nindent 4 }}
